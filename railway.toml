[build]
builder = "NIXPACKS"

[deploy]
startCommand = "mkdir -p /app/prisma && npx prisma db push && npm run start"
healthcheckPath = "/api/vendors"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
